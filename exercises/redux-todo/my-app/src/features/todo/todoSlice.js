import { createSlice } from "@reduxjs/toolkit";

export const todoSlice = createSlice(
    {
        name:"todoChecker",
        initialState: {
            isChecked: false
        },
        reducers:{
            
        }
    }
)